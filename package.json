{
  "name": "all-encompassing-translate-me",
  "version": "0.1.0",
  "private": true,
  "description": "AI-only bilingual web page translator. Bring your own DeepSeek / OpenAI key.",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare",
    "icons": "node scripts/build-icons.mjs",
    "install-local": "pnpm build && node scripts/install-local.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.36",
    "sharp": "^0.34.5",
    "typescript": "^5.9.3",
    "wxt": "^0.21.4"
  }
}
